using System;
using System.Collections.Generic;
using System.Linq;
using System.Security.Cryptography.X509Certificates;
using System.Text;
using System.Threading.Tasks;

namespace MoECapacityCalc.Exits
{
    public class ExitFactory
    {
        /*public static IExit CreateExit(ExitType exitType)
        {
            switch(exitType)
            {
                case ExitType.exit:
                    return new Exit();
                case ExitType.storeyExit:
                    return new Exit();
                case ExitType.finalExit:
                    return new Exit();

                default:
                    throw new NotSupportedException(
                        $"{exitType} is not supported."
                    );
            }
        }*/
    }
}
